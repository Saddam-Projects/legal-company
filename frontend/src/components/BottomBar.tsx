import ContainerComponent from './Container';

export default function BottomBarComponent() {
  return <ContainerComponent className="fixed bottom-0 left-0 w-full bg-white border-t-1 border-t-gray-100 h-14"></ContainerComponent>;
}
